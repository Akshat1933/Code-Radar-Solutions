#include <stdio.h>
#include <ctype.h>  

int main() {
    char ch;
    scanf("%c", &ch);
    if (isalpha(ch)) {
        ch = tolower(ch);
    }
        
    else if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u') {
        printf("%c", );
        } 
    else if {
        printf("%c", );
        }
    
    else if (isdigit(ch)) {
        
        printf("%c", );
    } 
    else {
      
        printf("%c", ch);
    }

    return 0;
}
