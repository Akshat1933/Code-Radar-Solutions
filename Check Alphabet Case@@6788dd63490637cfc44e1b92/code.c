#include <stdio.h>


int main() {
    char ch;
    scanf("%c", &ch);
    if(ch = A)
    else if(ch = a){
        printf("Lowercase");
    } 
    else(ch = a){
        printf("Not a alphabet");
    }

    return 0;
}

