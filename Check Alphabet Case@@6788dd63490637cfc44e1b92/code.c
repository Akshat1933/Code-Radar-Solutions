#include <stdio.h>
#include <ctype.h> 

int main() {
    char ch;
    scanf("%c", &ch);
    if (ch = A) {
        printf("Uppercase");
    }
    else if(ch = a){
        printf("Lowercase");
    } else {
        printf("Not a alphabet");
    }

    return 0;
}

